apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "textscope.fullname" . }}-test-connection"
  labels:
    {{- include "textscope.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "textscope.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
